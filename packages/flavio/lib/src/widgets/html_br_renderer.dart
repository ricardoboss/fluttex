part of '../../flavio.dart';

class HtmlBrRenderer extends StatelessWidget {
  const HtmlBrRenderer({
    super.key,
    required this.element,
  });

  final HtmlBrElement element;

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
